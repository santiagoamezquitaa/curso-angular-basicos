import { Component } from '@angular/core';

@Component({
  selector: 'app-root', //Es el nombre que tendrá este componente. Es el mismo que aparece en el index.html
  templateUrl: './app.component.html' //Si hay una contraparte de html se puede llamar mediante templateUrl o template solo y escribir el html como string
})
export class AppComponent {

}
